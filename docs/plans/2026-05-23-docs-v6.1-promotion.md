# Docs v6.1 Promotion

## Goal

Promote the current approved source-of-truth markdown set from `v6.0` to
`v6.1` after the latest cross-office staffing, approval worklist, and report
refinements.

## Scope

- create the full `v6.1` markdown source-of-truth set from the current
  repository-aligned `v6.0` content
- repoint `AGENTS.md`, `README.md`, and `PLANS.md` to `v6.1`
- keep `v6.0` as the immediately previous historical source-of-truth version

## Expected Deltas

- cross-office staffing create UX and employee-detail visibility refinements
- Office / BU Time Summary project-split behavior and cross-office attribution
- Approval Worklist naming/layout polish
- Project Time weekly summary grid and Pending Approvals column refinements

## Validation

- confirm the full `v6.1` markdown source-of-truth set exists
- confirm repo guidance points to `v6.1`
- confirm the pending code/test/doc changes are committed together
